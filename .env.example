# Legacy single repository configuration (still supported for backward compatibility)
MARKDOWN_ROOT_DIR=./mock-data/filesystem

# Multi-repository configuration
# Configure multiple markdown repositories by adding numbered environment variables
# Each repository needs both a path (MARKDOWN_ROOT_DIR_N) and a label (MARKDOWN_ROOT_LABEL_N)
# The system will automatically detect and use multi-repository mode if these are present

# Repository 1
# MARKDOWN_ROOT_DIR_1=/path/to/first/repository
# MARKDOWN_ROOT_LABEL_1=Main Documentation

# Repository 2
# MARKDOWN_ROOT_DIR_2=/path/to/second/repository
# MARKDOWN_ROOT_LABEL_2=API Documentation

# Repository 3
# MARKDOWN_ROOT_DIR_3=/path/to/third/repository
# MARKDOWN_ROOT_LABEL_3=User Guides

# S3 Configuration for media uploads
S3_BUCKET=your-s3-bucket
S3_REGION=your-s3-region
S3_ACCESS_KEY_ID=your-s3-access-key
S3_SECRET_ACCESS_KEY=your-s3-secret-key

# Development settings
USE_MOCK_API=false

# AI Integration
GEMINI_API_KEY=your-gemini-api-key